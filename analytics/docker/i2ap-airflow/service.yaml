apiVersion: v1
kind: Service
metadata:
  name: airflow-service
  namespace: i2ap
spec:
  type: NodePort
  selector:
    app: airflow
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: airflow
